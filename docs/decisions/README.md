# Decision log

Decisions taken during discovery and design live in the decision table of [docs/DESIGN.md](../DESIGN.md), each with its reason. From the first increment onward, every change to those decisions gets a dated file here, `NNNN-short-title.md`, stating context, decision, and consequences.

| Record                                                                                                           | What it covers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [0001 — Decisions taken while building Plan 1](0001-plan-1-build-decisions.md)                                   | The twenty-six rulings made during Plan 1's unattended execution. Five encode non-obvious Statistics Sweden data semantics (pre-existence zeros, the following-January boundary rule, per-cell perturbation, renumbered municipality codes, the enforced map-to-data join); the rest correct defects in the plan itself or close silent-failure paths. Two items are parked with reasons.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| [0002 — How "places like this" is computed](0002-similarity-metric.md)                                           | The six decisions behind Plan 6's distance metric, each settled by measuring rather than arguing: all ten indicators equally weighted, a ten-year window fixed across the slider, a log transform for the four skewed indicators, missing cells reducing the dimensions rather than the value, five neighbours presented as a set because the fifth-to-sixth gap has a median of 0.032, and prose that tells a municipality's own story. Two consequences are recorded as limitations rather than fixed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| [0003 — How the facts strip finds its own facts](0003-the-facts-engine.md)                                       | The six decisions behind Plan 7's facts engine. Five families with no score comparing them, because "how surprising" has no honest exchange rate across kinds of claim; leave-one-out for the family that uses Plan 6's neighbours, which closes a circularity the ordering decision did not anticipate; and a noise bound expressed in each indicator's own unit after three PEOPLE was briefly applied as three PERCENTAGE POINTS. Records that Plan 6's neighbours did not, on their own, solve the problem they were ordered first to solve.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| [0004 — How the morph is built](0004-the-morph.md)                                                               | The seven decisions behind Plan 8. Both questions DESIGN section 9 parked are closed by measurement: no Canvas layer (32-point SVG holds 60fps at 6x CPU throttling with 40% of the frame spare, and Canvas would mean two renderers forever beneath an accessibility layer that has to exist anyway), and bubbles rather than hexagons, which turned out not to be a performance question at all. Records that the morph has no unit-test coverage and cannot have any, because jsdom implements no path measurement.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| [0005 — A page per municipality](0005-a-page-per-municipality.md)                                                | The seven decisions behind Plan 9. The roadmap assumed SVG would do for a preview image; no platform documents SVG support, so the cards are rasterised. The path is `name-code` because å→a makes Håbo and Habo the same word. `?m=` keeps working for ever. Records that the previews cannot work until the project has a domain, and that the preview-server 404 was wrong in an instructive way the first time.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| [0006 — The name](0006-the-name.md)                                                                              | Atlas 290, chosen by the architect from a checked shortlist. The name had been open since day one and became load-bearing when Plan 9 shipped 290 preview cards that cannot render without an absolute origin. Records that the origin is free (`<project>.pages.dev`) rather than a paid domain, that "Sverige i siffror" is SCB's own and fails the one hard constraint, and that the Pages project name IS the hostname — so it is tied to `SITE_ORIGIN` in one variable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| [0007 — The 404 that was not](0007-the-404-that-was-not.md)                                                      | The first deploy revealed that production answered 200 with the root page for every unmatched path, because Cloudflare Pages treats a site with no top-level `404.html` as a single-page application. Corrects 0005 D6. The lesson is the reusable part: the test that asserted otherwise was checking our own simulation of Cloudflare against the assumption it was built from, so it could not have failed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| [0008 — Response headers, and how these pages get found](0008-headers-and-discoverability.md)                    | The four decisions from the post-0006 review. A sitemap, because every page this site serves is a shell and a crawler that does not run JavaScript finds no links anywhere — the preview cards make a PASTED link show the place and do nothing to make an unpasted one findable. A Content Security Policy that hashes the root page's inline redirect rather than allowing inline script, served by the preview server so the browser suite exercises the real policy, and proven able to fail with a corrupted hash. Records that turning the policy on found a real `new Function` in the bundle (zod, feature-detecting JIT), and that the performance budget had been measuring a build nobody visits — SEO 91 for want of the canonical and hreflang the deploy writes and the budget did not. Also records why the budget stopped passing by luck: its floor of 80 sat inside the measured CI spread of 79-85, so it now takes a median of three runs and sits clear of it.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| [0011 — The profile takes the page](0011-the-profile-takes-the-page.md)                                          | Supersedes 0010's D1 and only that. The profile gets a full-width row of its own in the layout grid, under the map and before the facts, because a measure row is a name, a figure and a trend and at column width the name and the figure fought for the same line. Records that 0010 read the defect one step too literally: what was wrong was never that the profile sat below the map, it was that it sat below five facts. Also records two bugs the change found — two grid items given the same area are drawn on top of each other, and an auto side margin turns off a grid item's stretch, which put 486 px of comparison table inside a 320 px screen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| [0010 — The place beside the map](0010-the-place-beside-the-map.md)                                              | The seven decisions behind Plan 11, which answers five defects the architect found by looking at what 0009 built. The profile moves into the reading column beside the map, so a click changes something a visitor can see; the table takes the map's box and scrolls inside it, down from a document 13,342 px tall to 1,331; a shape under the pointer says its own name using the same two strings its accessible name is built from, and stays out of the accessibility tree because the value is already there. Records that the skip link had been silently obscuring the play button all along, invisible and first in the hit test, and that only a page which started scrolling on open revealed it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| [0009 — The design language](0009-the-design-language.md)                                                        | The eleven decisions behind Plan 10, settled from four mockups drawn on the real pantry. The map ground is fixed white, so the page ground is never white in either theme and the map reads as a printed sheet — the constraint became the organising idea. Light is now the standard, with a designed dark theme and a control that beats the operating system in both directions. Records that cutting the variable weight axis to 400-600 is what made three self-hosted faces fit under a 120 kB budget, that performance fell to 83-84 and why, and the three defects that only looking found.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| [0016 — The fifteen](0016-the-fifteen.md)                                                                        | The eight decisions behind Plan 16, which takes the pantry from ten indicators to twenty-seven. Records that reading the tables changed six of the fifteen the design named: fertility has no sex total and summing one would not be a fertility rate, the labour tables have no age total so 20–64 is chosen and named, disposable income is already in fixed prices and is not adjusted again, the tax base loses its 2026 rather than putting one nominal figure at the end of an adjusted series, rent is per square metre so the id says so, and two units joined the contract because `count` rounds a fertility rate of 1.45 to 1. Seventeen indicators ship rather than fifteen, because the education gap cannot be checked unless the two splits it is built from are published too. Records two defects breadth found and one left deliberately unfixed: a single series ending in 2022 silently pulled every extreme fact back two years, two similarity guards read every indicator where they should read the core ten, and the country fact is now decided by the last tie-break tier for the first time. Also records that Bjurholm has no fertility rate in any year, that the design's freeze risk closed at 3.1 MB against a feared 48, and that twenty-seven sparklines cost four Lighthouse points on a municipality page and none on the root.                                             |
| [0015 — The old implementations go](0015-the-old-implementations-go.md)                                          | The six decisions behind Plan 15, which closes what 0014 D7 deferred. The nine hand-written indicator implementations are deleted — 3,753 non-test lines down to 2,651, and the nine modules themselves halved — and the roughly 190 assertions against them were sorted rather than dropped: twenty-eight that were derived independently of the pipeline moved to the published pantry, thirty table-specific ones were retargeted at the declarations, and the rest went because the shared resolver now makes those claims generically. Records that the deletion found a live guard plan 14 had left behind in dead code, so post-secondary-education had been running without its codelist check; that this project's memory of two TAB1169 labels was wrong; and that reading OBSERVATION_STATUS from memory rather than from shared/pantry.ts turned four cells into plausible nonsense without erroring. States plainly what the surviving net cannot do — a definition compared to a pantry it generated proves no drift, never correctness — and why that matters for the fifteen.                                                                                                                                                                                                                                                                                                                    |
| [0014 — Indicators become definitions](0014-indicators-become-definitions.md)                                    | The seven decisions behind Plan 14. An indicator is now a declaration of 7 to 30 lines against modules of 153 to 696, over three shared builders, and `public/pantry/` is byte-identical after every one of the nine migrations. Records that builders arrived with the indicator that needed them rather than in advance, that two of the five the plan imagined were never needed, and that `population-change` stays hand-written because a generic builder for it would have one user. Also records two defects found by building: an age rule that would have counted the over-65s twice, caught by the frozen-response layer, and a published method description that named indicators the metric never looked at. The dead per-indicator code is deliberately left for its own change, because this one claims nothing moved and a 1,500-line deletion would bury the proof.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| [0013 — The pantry splits](0013-the-pantry-splits.md)                                                            | The six decisions behind Plan 13. One file of 272,475 gzipped bytes became an index of 6,433 plus one file per indicator, so first paint costs about 35,301 rather than every cell of all ten indicators for a view that draws one — and the index barely grows with the count, because the 290 municipalities dominate it. Two decisions were found by building rather than planning: the site cannot hold a whole `PantryData` once series load lazily, because an unfetched indicator has no prose, and the year axis had been derived from the loaded series, which would have given the slider a different length depending on which indicator a visitor arrived on. Records that the profile's cost moved rather than disappeared, and that the 290 preview cards coming out byte-identical is the end-to-end proof that the split lost nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| [0012 — Releases, and a changelog generated from commits](0012-releases-and-a-changelog.md)                      | The seven decisions behind Plan 12. A version number that means one thing — MAJOR is a published URL that stopped working, and nothing else — because the URL grammar is the only promise this project makes outward. The monthly SCB refresh cuts no release: it already arrives as a reviewable pull request, and a version would otherwise claim to describe data it does not describe. Entries are generated by release-please from commit subjects, which only works if the subjects are conventional — checking the regex against every merged pull request found that **none of the nineteen** would have passed, including the squash-merged title carrying the whole of Plan 11, so a title check is part of the decision rather than an extra. Records plainly what the default `GITHUB_TOKEN` gives up: no CI on the release pull request.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| [0021 — Stage A, and what the tables said](0021-stage-a-and-what-the-tables-said.md)                             | The seven decisions behind Plan 17, which takes the pantry from twenty-seven indicators to thirty-five. Splits the fourth slice's deferred question in two and shows only half is hard: every sparse table publishes plain years, so sparsity is presentation rather than contract, and only two tables key by something that is not a year. Pinning each five-year window to its last year turns life expectancy into a DENSE series, because the windows overlap by four. Records that a content code existing in the metadata is not the same as that code having values — all five of TAB4422's share codes are empty at municipal level, so Q25 became a distance in metres — that SCB already publishes cars per 1,000 so it is not a rate this project computes, and that a source may now name several contents and is grouped by label because three stitched commuting tables use three codes for one measure. Also records the cell that proves the round-once rule, and an assertion the data corrected: women do NOT outlive men in three windows, in the two smallest municipalities involved, which is the caveat's own argument.                                                                                                                                                                                                                                                                 |
| [0017 — The flake was a security header](0017-the-flake-was-a-security-header.md)                                | Issue #26, closed: the browser suite lost a different test every run because `Cross-Origin-Opener-Policy: same-origin` swaps the browsing-context group on a page's first navigation away from `about:blank`, and Firefox's Playwright driver intermittently loses that navigation — `page.goto` never returns although the document is complete and its `load` event has fired. Found by bisecting the response headers against a bare server: no headers 0/150, the CSP alone 0/150, the other four 0/150, COOP alone 29/150. Explains every property the report recorded, including why no test was ever the culprit: every test navigates once, and only the first navigation is at risk. Firefox stops enforcing the policy in the test browser; the site never stops serving it. Also widens the five axe scans that had not been marked slow, makes the morph's frame sampler wait for frames rather than for a clock, and lowers its assertion to the claim it actually makes. Records that the WebKit strand is NOT closed, and why this machine cannot close it.                                                                                                                                                                                                                                                                                                                                       |
| [0018 — The focus a link never asked for](0018-the-focus-a-link-never-asked-for.md)                              | Issue #32, closed, and with it the WebKit strand ADR-0017 left open. The cause was not Playwright's `.focus()` and not Linux WebKit: the profile panel moved focus to its own heading whenever it appeared, arrival included, and on a deep link that landed an unpredictable moment after the page was readable — stealing focus from the test that had just focused a shape, and from any visitor who had started tabbing. Reproduced on every engine by delaying that focus 300 ms, which turns all three CI failures red with the same messages. Neither suggested fix is taken: more WebKit retries would have hidden a real site defect, and rewriting the eight `.focus()` call sites would have removed the only thing reporting it. Records that the worse half was never the flake — the heading sits below the map, so an arriving keyboard visitor was dropped past the skip link, the search, the year and the map with no way forward — that Escape no longer closes a profile the instant a deep link arrives, and that one cause found is not a suite proved clean.                                                                                                                                                                                                                                                                                                                              |
| [0019 — The view is built once](0019-the-view-is-built-once.md)                                                  | Issue #35, closed. A municipality page scored 68 against a floor of 72, and the cause was the one ADR-0013 predicted and deferred to a plan that did something else: `viewOf` re-ran `PantryView.parse` over every already-loaded series each time one more arrived, so a profile open re-validated the same cells thirty-five times — 1,023 ms, now 1 ms. Validation moves to the boundary where the bytes are untrusted, and only the cross-reference check that relates the parts to each other still runs per call; five tests pin each check that went with it. Records that BOTH mechanisms the issue named as plausible — a discarded rank cache and thirty-five sparklines — do not appear in a CPU profile of the real page at all, that zod was ~3,700 ms of ~4,900, and that the "unattributed 2.9 s" was a unit error: the benchmark runs unthrottled and Lighthouse throttles 4x. Also closes the second finding — the 580 shareable pages had never been measured by anything, so 86 and 82 in earlier slices were above the floor by luck — by making `yarn budget` measure a municipality page by default, in the same change as the fix and not before it.                                                                                                                                                                                                                                      |
| [0020 — Sparsity is a presentation problem](0020-sparsity-is-a-presentation-problem.md)                          | The nine decisions behind Plan 19, which takes the pantry from thirty-five indicators to forty-two and teaches the site about holes. `coverage.years` is written only when a series is not a dense run, so thirty-five indicators carry nothing new; one predicate replaces the range check the site had written out three times; and the build now refuses a declaration that does not match its series in EITHER direction, including the dangerous one where a sparse series declares nothing and ships looking dense. Records that the design's `councillor-gap` does not exist — `000000BO` is null for all 1,450 cells at the background total — which is ADR-0021's content-code trap for the second time in one slice, and that the councillor total had to be chosen by checking because the table's seven "samtliga" codes do not agree. Records two caveats this plan wrote wrong and the published figures corrected, the four points where municipal turnout BEAT the general election, and two defects only looking found: the year-tick strip has never been visible since plan 3, and the kicker was claiming the axis as each measure's own span. Holiday homes are deferred because no OBSERVATION_STATUS honestly describes 106 municipalities that have no holiday-home area at all. Seven more indicators cost nothing on the municipality page — 92 to 95 — because ADR-0019 landed first. |
| [0022 — The reference scale is not built](0022-the-reference-scale-is-not-built.md)                              | Stage C of the fifth slice, and the end of `scale.reference`. Both of its values were dead in different ways: `'zero'` was declared by seven indicators and read by nothing, because the zero those scales mark is derived from `kind: 'diverging'` and never from the field, and `'national-median'` had no producer and no consumer. Records why the median scale is not built rather than merely not built yet — the breaks are quantiles FIXED across every year, which is what stops a municipality's colour moving while its value stands still, and a national median moves, so colouring against it would do the exact thing fixed breaks exist to prevent. No schemaVersion bump, because removing an optional field is compatible in both directions, unlike plan 17's rename. Records that Q30 is therefore still unanswered and that the fourth slice's "not an indicator, it is a scale" was the reclassification this record undoes. Also records the defect removing the field uncovered: `councillors-women-share` was declared diverging in plan 19 and runs 24 to 58, so the legend was marking a zero twenty-four points outside the data — now sequential, with a test that requires every diverging indicator's values to actually straddle zero. Not one of the 42 published series changed.                                                                                               |
| [0023 — What absence means, and how a place compares to the country](0023-what-absence-means-and-the-country.md) | The six decisions behind Plan 21, which closes both items the fifth slice deferred and takes the pantry to forty-four indicators. `OBSERVATION_STATUS` gains a seventh value, `nothing-to-count` — the thing this measure counts does not exist here, as against a figure that exists and is unpublished — because 106 municipalities have no holiday-home area and `not-yet-published` would have promised them a figure that is never coming. Q30 ships as an indicator rather than the colour scale the fourth slice imagined, named `share-65-plus-vs-country` for what it computes: the question list's title asks about ageing SPEED and its own formula about a LEVEL, only the formula was specified, and naming it after the title would have been the more appealing claim and the false one. Records that the national share is population-weighted and that this is worth four percentage points — 20.84 against an unweighted 25.07 in 2024 — which is the failure this indicator was likeliest to have and so the one the tests are built around, and that 20.84 and 13.39 match SCB's own national figures, the only check here that is not circular. Also records a Sparkline test that refused to be loosened and was made more precise instead, and that the municipality page fell 95 to 90.                                                                                                  |
| [0024 — Bubbles sized by the measure](0024-bubbles-sized-by-the-measure.md)                                      | The seven decisions behind Plan 21, which changes what a bubble's size means: the measure on screen, not population. One Dorling layout per indicator, inside that indicator's file, because size cannot follow the value on positions solved for population radii; a slot is the largest bubble a municipality takes in any year, so the slider changes sizes and never positions; size is normalised WITHIN the year while colour keeps its fixed breaks, because normalising across years made the 2026 tax rate a field of identical dots; every layout spends 13% of its box, because without a budget the first prototype drifted 300–460 units into a shapeless blob; the floor is eight units, because at six seven layouts stranded municipalities from the arrow keys; the kitchen proves each layout reachable and publishes the cone, refusing to publish one it cannot. Records three defects the work found, including a language switch that had matched no CSS rule since Plan 10.                                                                                                                                                                                                                                                                                                                                                                                                               |
| [0025 — The editorial pass](0025-the-editorial-pass.md)                                                          | A screen-by-screen review of the finished site, in both languages, both themes and four widths. Three indicators that subtract one share from another were published in `percent` while their own descriptions said percentage points, so the 65+ gap read "−5,64 %"; the contract gains `percentage-points` and no figure moves. Unit words move to `shared/` so the facts stop quoting bare numbers; Swedish spaces its percent sign. The profile becomes a standfirst and a body on wide screens, the phone gets its own compare, row and bar layouts, absences are set as absences, the theme button stops claiming a pressed state its name contradicts, and `yarn dev` can serve a municipality page again. Records that the spaced em dash is left alone, and why.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
